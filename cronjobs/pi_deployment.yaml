apiVersion: batch/v1
kind: Job
metadata:
  name: yamlpi-github-commmit
  namespace: 'qa-automator'
spec:
  template:
    spec:
      containers:
      - name: pi
        image: perl
        command: ["perl",  "-Mbignum=bpi", "-wle", "print bpi(500)"]
      restartPolicy: Never
  backoffLimit: 5
